modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Hotel Booking API Server"

[[workflows.workflow]]
name = "Hotel Booking API Server"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "npm install express mongoose bcryptjs jsonwebtoken cors dotenv joi && node server.js"
waitForPort = 8000

[deployment]
run = ["sh", "-c", "npm install express mongoose bcryptjs jsonwebtoken cors dotenv joi && node server.js"]
